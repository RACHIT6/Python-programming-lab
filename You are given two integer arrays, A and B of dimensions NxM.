# Your task is to perform the following operations:
# 1. Add (A +B )
# 2. Subtract ( A-B )
# 3. Multiply ( A*B )
# 4. Integer Division (A //B )
# 5. Mod ( A%B )
# 6. Power ( A**B )

row = int(input("Enter row of array: "))
col = int(input("Enter column of array: "))

arr = np.eye(row, col)
print(arr)
